// Hallo, this is my Numeric Method using table method
#include <stdio.h>
#include <conio.h>
#include <math.h>
define maks 10000000;

double f(double x){
  return (((x*x) - (pow(exp(1),x)))-4);
}

int main(){
  print("Program Metode Numerik Tabel");
  system("pause");
  return 0;
}
